#!/bin/sh
if ! test $(grep pre-upgrade.01-defaultSparseCheckout $GIT_DIR/info/sparse-checkout); then
    cat >> $GIT_DIR/info/sparse-checkout << EOF
# from pre-upgrade.01-defaultSparseCheckout
*
EOF
fi
